- author = !author ? req.user : author
a.circle-img(href='/users/#{author.name}')
  .img.circle-img-show
    img.avatar-max(src="/avatar/#{author.name}")
  .circle-img-info
    .circle-img-info-text #{author.name}