include article-title
.post-short
  .post-short-right
    - if(typeof article.brief !== 'undefined' && typeof article.brief.img !== 'undefined' && article.brief.img !== '')
      img(src='#{article.brief.img}')
  | #{article.brief.text}