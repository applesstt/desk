- if(pages > 1)
  .text-center
    ul.pager
      - if(page > 1)
        li.previous
          a(href = '?page=#{page - 1}') ← 上一页
      - if(page < pages)
        li.next
          a(href = '?page=#{page + 1}') 下一页 →
